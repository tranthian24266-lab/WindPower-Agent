你现在接手项目 `C:\Users\luzian\Desktop\windpower_agent3` 的智能体工程化改造。

先完整阅读：

- `C:\Users\luzian\Desktop\windpower_agent3\docs\agent-execution-packages-plan-2026-06-16.md`
- `C:\Users\luzian\Desktop\windpower_agent3\docs\agent-must-do-checklist-2026-06-16.md`

本次只执行：

- `执行包 A：统一 run 体系 + 多步骤 runtime 拆解`

执行要求：

1. 只做执行包 A 以及它的直接依赖，不要提前做执行包 B/C。
2. 开始前先说明本次要改哪些文件、目标是什么。
3. 优先复用现有 `run_manager`、`step_executor`、`review_service`、`vector_index_service`，不要重写系统。
4. 不要修改 `C:\Users\luzian\Desktop\littlemodel` 内的任何代码、权重和测试数据。
5. 保留现有同步接口兼容性；优先新增异步 run 能力，而不是粗暴替换旧接口。
6. 至少把以下 run type 纳入统一 run 体系：
   - `diagnosis`
   - `knowledge_reindex`
   - `review_publish`
7. 把 `chat_answer` 和 `enhanced_report` 从粗粒度单 step 拆成更细的多步骤工作流，并能在 run detail 中看到 timeline。
8. 如需数据库迁移，请按现有序号风格新增迁移文件，不修改历史迁移。
9. 完成后必须运行并汇报这些验证：
   - `pytest -q backend/tests/test_agent_runs_api.py backend/tests/test_agent_async_runs.py backend/tests/test_run_timeline_api.py backend/tests/test_diagnose.py backend/tests/test_knowledge_api.py`
   - `pytest -q`
   - `cd frontend && npm run build`
   - `cd frontend && npx playwright test`
10. 完成后更新相关文档中的状态说明，但不要虚报完成项。

本次完成的最低标准：

- `POST /api/agent-runs` 可接受新增 run type
- 新增 run type 可入队执行
- `RunDetailPage` 能看到更细的 step timeline
- `DiagnosisPage` 和 `KnowledgePage` 能跳转或关联到 run detail
