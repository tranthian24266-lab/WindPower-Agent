# models

This module keeps the deployed checkpoint in `weights/`.

`models/` is reserved for future locally retrained PCA-CNN or MSCNN-BiLSTM artifacts when the upstream training project is integrated into this repo.
