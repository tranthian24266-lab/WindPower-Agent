# models

This module keeps the deployed SVR payload in `weights/`.

`models/` is reserved for future GRU or alternate baseline artifacts if the notebook reproduction is converted into a trainable in-repo workflow.
