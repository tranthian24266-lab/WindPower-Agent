{
  "default_input": "test_data/test_data_sample.csv",
  "allow_stats_columns": false,
  "external_data_env": "ANOMALY_DATA_DIR",
  "notes": [
    "Default CARE2Compare demo uses one CSV file only.",
    "Default preprocessing keeps Avg/average/mean style columns and excludes Min/Max/Std unless explicitly enabled."
  ]
}
